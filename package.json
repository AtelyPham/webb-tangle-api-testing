{
  "name": "webb-tangle-api-testing",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "esr index.ts --bundle --outfile=index.js --format=cjs --platform=node",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "AtelyPham",
  "license": "ISC",
  "dependencies": {
    "@polkadot/api": "10.9.1",
    "@webb-tools/tangle-substrate-types": "^0.5.1"
  },
  "devDependencies": {
    "esbuild": "^0.19.10",
    "esbuild-runner": "^2.2.2"
  }
}
